// Package common defines shared constants used across the provider.
package common

const (
	// ProviderName is the canonical name of this provider.
	ProviderName = "provider-example"

	// TODO: Define component names matching those in provider-config.yaml.
	// Example:
	// ComponentDatabase = "provider-example-db"
)
