{{- if .Values.rbac.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "provider-example.fullname" . }}
  labels:
    {{- include "provider-example.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "provider-example.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "provider-example.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
