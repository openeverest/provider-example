# Every node passes its readiness probe, so Status() reports Ready and the
# runtime publishes the connection Secret.
apiVersion: core.openeverest.io/v1alpha1
kind: Instance
metadata:
  name: cache
status:
  phase: Ready
